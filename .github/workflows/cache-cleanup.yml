# Cache Cleanup — weekly prune of stale GitHub Actions caches

name: Cache Cleanup

on:
  schedule:
    - cron: '17 6 * * 0'  # Sunday 6:17am UTC
  workflow_dispatch:

jobs:
  cleanup:
    uses: AlbertvD/homelab-platform/.github/workflows/cache-cleanup.yml@ddaa7e74146f38b11ecc28ee1a41a8eb411f30d9 # v1
    with:
      runner: self-hosted
