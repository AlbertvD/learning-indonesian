name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  safety:
    uses: AlbertvD/homelab-platform/.github/workflows/safety.yml@main
    secrets: inherit

  dependencies:
    uses: AlbertvD/homelab-platform/.github/workflows/dependency-audit.yml@v1
    with:
      fail_on_vulnerabilities: false
