---
status: approved   # was approved (2026-06-02 for A+B+C); reverted to draft 2026-06-03 after live-DB grounding triggered the OQ3-3 off-ramp (item cloze DEFERRED); re-scoped to (A)+(B) and re-approved 2026-06-03 after fresh dual review.
epic: "#98"
issue: "#101"
depends_on: "#99 (PR #121, shipped) + #100 (PR #124, shipped) — the DB→DB spine, the in-stage generator pattern, the Capability Gate, and the idempotent writers"
supersedes: []
grounded_against_code: 2026-06-02
grounded_against_db: "DONE 2026-06-03 (openbrain live, run from the slice-3 worktree session). Findings BIND: (1) OQ3-3 off-ramp TRIGGERED — ZERO item-sourced contextual_cloze caps and ZERO cloze_mcq-typed caps exist; only dialogue_line contextual_cloze exists (43 caps, all ready/published). 1,167 item_contexts(cloze) rows across 699 items are orphaned (no cap points at them). Item cloze is forward-declared in renderContracts.ts but never emitted (projectors/vocab.ts:182 hardcodes sourceKind='dialogue_line'; runner.ts:657-703 calls item cloze 'deferred'). DECISION (operator, 2026-06-03): DEFER item cloze (C) to its own slice — it is a feature build, not a migration. (2) R6 dedup check: 0 duplicate (learning_item_id, source_text) rows. (3) Baselines: 41 dialogue_clozes rows vs 43 dialogue_line contextual_cloze caps → a 2-cap coverage gap to confirm at build (HC15 / Task 8 territory)."
reviewed_by:
  # ROUND-1/2 sign-off (2026-06-02) was for the (A)+(B)+(C) plan — SUPERSEDED. Fresh dual re-review of the 2026-06-03 (A)+(B) re-scope below.
  - "architect: APPROVED-WITH-NITS 2026-06-03 (re-scope review). Verdict: re-scope architecturally coherent — coherent-unit deferral correct, dialogue/item consumer split sound, no fold drift, three-gate discipline intact. MAJOR-1 (R4 risk resolvable at desk before Task 8, not at live trial) + MAJOR-2 (no-disk test needs positive-removal assertion + stale-comment fix) + MINOR-1 (Task 10 global-vs-single-lesson gap wording) — ALL FOLDED IN."
  - "data-architect: APPROVED-WITH-NITS 2026-06-03 (re-scope review). Verdict: no CRITICAL/MAJOR; R1 deferral confirmed zero-42P10-risk in Slice 3 (item_contexts untouched; the 1,167 rows wrote via the INSERT branch, ON CONFLICT never evaluated — latent risk correctly deferred); dialogue translation triangle intact; affixed canonical-key stability correct; no shape drift. m-1 (extend DialogueClozeInput + replaceDialogueClozes for translation_nl/en) + m-2 (spell out M-3 source_ref query+compare) — BOTH FOLDED IN."
decisions_resolved:    # from the 2026-06-02 grill-with-docs session — these BIND
  OQ3-1: "SCOPE = full DB→DB conversion, NOT artifact-cleanup-only. Issue #101's wording ('cleanup') is under-scoped vs CONTEXT.md → Stage Contract (line 115), which forbids the disk read and names cloze-contexts.ts. Slice 3 removes the staging READS for these kinds + the artifact writes. Update issue #101."
  OQ3-2: "REVISED 2026-06-03 — TWO concerns in Slice 3, not three. cloze-creator writes ONE file (cloze-contexts.ts) mixing item carrier sentences AND dialogue clozes (lint-staging.ts: checkClozeCoverage reads it for word/phrase items, checkDialogueClozes for dialogue lines). Slice 3 = (A) dialogue_line cloze [dialogue_clozes, exists] + (B) affixed_form_pair repoint [lesson_section_affixed_pairs -> affixed_form_pairs]. (C) item vocab cloze is DEFERRED (see OQ3-3) and moves as a COHERENT UNIT — its cloze-contexts.ts read, item_contexts(cloze) write, cloze-creator item authoring, and the R1/R5/R6 machinery all defer with it. CONSEQUENCE: the dialogue cloze path is repointed to generate from lesson_dialogue_lines in-stage (Mode 2), so it no longer needs clozeContexts; the ONLY remaining consumer of cloze-contexts.ts is the deferred item path, so that file's read STAYS in Slice 3 (removed in the item-cloze slice)."
  OQ3-2-correction: "(historical, for the deferred item-cloze slice) (C) item vocab cloze persists to item_contexts (context_type='cloze') via upsertClozeContext (adapter.ts:613-632), NOT exercise_variants. Runtime already reads item_contexts (Decision R, PR 1; renderContracts.ts:110). (C) needs NO new DDL table and does NOT block Slice 4's exercise_variants drop. NOTE (2026-06-03 grounding): item cloze is forward-declared in renderContracts.ts (cloze accepts ['item','dialogue_line']; cloze_mcq serves item contextual_cloze) but NEVER EMITTED — projectors/vocab.ts:182 hardcodes sourceKind='dialogue_line'; runner.ts:701-703 says 'no contextual_cloze cap in the base item caps... when the item cloze cap is added, this block writes it.' So (C) is a DEFERRED FEATURE BUILD (emit the cap + wire cloze_mcq_item_distractors + scheduling), not a migration."
  OQ3-3: "DEFER item vocab cloze (REVISED 2026-06-03 — operator decision; was MIGRATE). The off-ramp condition is met in its STRONGEST form: the openbrain ready-cap check shows ZERO item-sourced contextual_cloze caps (not merely not-ready — categorically absent). The original 'migrate' rested on the false premise that item contextual_cloze was a live, wired exercise with caps to move; the live DB + code prove it was forward-declared and deferred (see OQ3-2-correction). 'Migrate' would therefore mean BUILDING a net-new capability type, beyond this slice's budget and a product decision in its own right. Operator chose DEFER (not RETIRE) — the 1,167 authored carrier sentences are preserved as-is; the item-cloze feature is built in its own slice later. Slice 3 leaves the entire item path untouched. (At 2-user scale review_events are a weak signal; the decision rests on cap EXISTENCE, which is unambiguous.)"
  OQ3-4: "DISSOLVE #126 (do NOT depend on it). The in-stage cloze generator produces clozes (and skip decisions) for ALL lessons' dialogue lines from lesson_dialogue_lines (DB). Idempotent (ADR 0011): L6/L9's reviewed clozes are skip-if-exists; only missing L5/7/8 generate. RELOCATE the checkDialogueClozes gate from lint-staging (lesson-stage pre-flight) into the Capability Stage gate — cloze existence is a capability-stage post-generation invariant, not a staging pre-flight fact. Net: L5/7/8 publish lesson content freely -> stage generates their clozes -> #126 disappears, no manual cloze-creator run. NOTE: this couples Slice 3 to the lint-staging decomposition (Lesson-Gate epic #109)."
  OQ3-5: "patternSourceRef = STAGING-ONLY (option b). No pattern_source_ref column on affixed_form_pairs. No runtime reader joins grammar_patterns (byKind/affixedFormPair.ts reads only capability_id/root_text/derived_text/allomorph_rule); allomorph_rule already carries the explanation inline. WRINKLE (precise, per m-1): 'men-active' exists in the MORPHOLOGY catalogue (MORPHOLOGY_PATTERN_SLUGS, projectors/morphology.ts:21) but there is NO grammar_patterns row with slug 'men-active' (L9's grammar_patterns are verb-ordering-abc/intensifier-position/benar-betul-truth-adverb) — so option (a)'s FK to grammar_patterns(slug) would have nothing to point at. Annotate CurrentAffixedFormPair.patternSourceRef as @stagingOnly."
  OQ3-6: "NO-DISK guarantee is SCOPED, not global. Slices 1+2 did PARTIAL cutovers: projectVocab(staging.learningItems) still emits audio_recognition/dictation + sentence/dialogue_chunk caps (runner.ts:361,409-410); projectGrammar runs for lessons without typed categories; content-units.ts/capabilities.ts/exercise-assets.ts still read + stagingWriteback still writes. REVISED 2026-06-03 (OPTION A — morphology read deferral): Slice 3 removes 0 of 5 canonical staging reads. Both cloze-contexts.ts AND morphology-patterns.ts STAY, because BOTH still feed CAPABILITY emission through the staging-derived buildCapabilityStagingFromContent regeneration (projectCapabilities) — the content-units/capabilities derivation this OQ explicitly defers to Slice 5. cloze-contexts.ts → deferred item-cloze path (projectCloze); morphology-patterns.ts → affixed cap emission (removing it would emit zero affixed caps → soft-retired). Slice 3 moves the dialogue cloze GENERATION + dialogue_clozes/affixed_form_pairs ROW DATA to the DB, but the staging-READ removal is entirely Slice 5. Epic #98 User Story 10 CANNOT pass at Slice 3. The enforcement test asserts the NEW dialogue/affixed code is disk-free (the walk); there is no read-removal to assert."
  OQ3-7: "affixed_form_pairs.allomorph_rule stays NOT NULL (audit I1). Treat 'no allomorphy' as a content value ('ber- attaches without sound change: jalan -> berjalan'), never NULL. Slice 3 only REPOINTS affixed (reads lesson_section_affixed_pairs); the NOT NULL is a lesson-stage authoring expectation enforced by the Lesson Gate. No Slice 3 code change."
new_slice_named:
  "Item-cloze slice (build the deferred feature — NEW 2026-06-03)": "Surfaced by the OQ3-3 deferral. Item contextual_cloze is forward-declared (renderContracts.ts) but never emitted. This slice BUILDS it: emit an item:contextual_cloze capability per eligible item (extend projectItemsFromTypedRows — the runner.ts:701-703 hook), port the cloze-creator Mode-1 carrier-sentence generator in-stage, write item_contexts(cloze) from DB-sourced items, wire cloze_mcq_item_distractors (runner.ts:657), add R1 (item_contexts UNIQUE(learning_item_id, source_text) index) + R5 (count-based 'seeded' predicate, N carrier sentences/item) + R6 (pre-DDL dedup check), then remove the cloze-contexts.ts read + relocate checkClozeCoverage/checkClozeContextsFile off lint-staging + retire cloze-creator. This is a FEATURE BUILD with a product question (do we want item-level cloze as a live exercise?), not a mechanical migration — scope it on its own. Depends on Slice 3's Mode-2 generator infra (extends it with Mode 1)."
  "Slice 5 (legacy-projection retirement)": "Surfaced by OQ3-6. Retire the staging-derived buildCapabilityStagingFromContent → projectCapabilities regeneration: extend the typed-row projectors to emit the residual cap kinds from the DB — audio/sentence item caps from lesson_section_item_rows, AND affixed caps from lesson_section_affixed_pairs (the Option-A 2026-06-03 deferral — affixed cap emission still reads morphology-patterns.ts via the regeneration); retire projectVocab/projectGrammar's staging path; kill content-units.ts/capabilities.ts/exercise-assets.ts derivation + stagingWriteback. THEN remove the residual loader staging reads (morphology-patterns.ts, cloze-contexts.ts, …) + de-allowlist loader.ts + flip the GLOBAL no-file-I/O gate (User Story 10) on. NOT covered by Slices 1-4. Without it the epic's no-disk goal silently never lands."
data_prerequisite: "dialogue_clozes (7 rows, L9) + affixed_form_pairs (4 rows, L9) already populated (data-model PRs 2/3). lesson_section_affixed_pairs + lesson_dialogue_lines populated by PR 6 for re-published lessons only (6/9 — L5/7/8 lint-blocked, the #126 keystone). The live trial MUST target a #126-blocked lesson (L5 or L7) to prove the dissolution."
---

# Capability Stage Redesign — Slice 3: `dialogue_line` cloze + `affixed_form_pair`, DB→DB

> **SCOPE NOTE (2026-06-03):** item vocabulary cloze, originally concern (C) of this slice, is **DEFERRED to its own slice** (see OQ3-3 + the live-DB grounding). This slice is now **(A) dialogue_line cloze + (B) affixed_form_pair repoint**. References to "item cloze" / "item_contexts(cloze)" / "two modes" below are retained only where they document the deferred work or its seams.

> **For Claude:** REQUIRED SUB-SKILL: superpowers:executing-plans. This extends the **shipped** Slice-1 (#99/PR #121) and Slice-2 (#100/PR #124) DB→DB spine. It is a **redesign, not a fold** (ADR 0011/0012/0013). **Read the Slice-1 and Slice-2 plans first** — this reuses their seams (`loadFromDb`, the in-stage generator pattern from `generateItemDistractors.ts`/`generateGrammarExercises.ts`, the idempotent writers, `runCapabilityGate`) and their hard-won live-trial lessons (they are requirements here, not history).

**Goal.** Convert `dialogue_line` cloze and `affixed_form_pair` to the DB→DB spine: read dialogue lines + morphology pairs from the typed lesson-content tables in the DB, **generate** dialogue cloze contexts in-stage (port the `cloze-creator` agent's dialogue contract — Mode 2), write `dialogue_clozes` / `affixed_form_pairs` ROW DATA from the DB, retire the `capability_artifacts` writes for these kinds, and **relocate the `checkDialogueClozes` gate** from `lint-staging` into the Capability Stage — which **dissolves #126** (L5/7/8's cloze block). Validated by a mandatory live trial on a #126-blocked lesson. *(OPTION A 2026-06-03: Slice 3 removes NO staging reads — both `cloze-contexts.ts` AND `morphology-patterns.ts` still feed cap emission via the Slice-5 regeneration, so their removal defers to Slice 5.)*

**Architecture.** Same deep-module shape — the database is the entire external interface. The import seam (`loadFromDb`) extends to read `lesson_dialogue_lines` + `lesson_section_affixed_pairs` (+ current dialogue-cloze/affixed cap state for the delta); generation (LLM, in-stage) produces dialogue cloze contexts (Mode 2); pure projectors fan out to `dialogue_clozes` / `affixed_form_pairs`; the idempotent export seam writes typed tables only. **`dialogue_clozes` is KEYED** `UNIQUE(capability_id)` — a per-dialogue-line generation gate (seeded iff a row exists, keyed 1:1 by `source_line_ref`) prevents redundant LLM calls and is the sole idempotency mechanism. *(The deferred item path's `item_contexts onConflict(learning_item_id, source_text)` key + its R1 index move to the item-cloze slice.)*

**Tech Stack.** TypeScript + Bun; Supabase JS (service-role) in `scripts/lib/pipeline/capability-stage/`; `@anthropic-ai/sdk` for in-stage generation (mirroring `enrichPos.ts` / `generateItemDistractors.ts`); Vitest; runtime readers in `src/lib/exercise-content/byKind/{dialogueLine,affixedFormPair}.ts` + `byType/{cloze,clozeMcq}.ts` (LOCKED — already DB-sourced from PRs 1-4).

---

## Implementation reconciliation (post review round 1 — 2026-06-02 — BINDS over body prose)

> **RE-SCOPE BANNER (2026-06-03 — item cloze DEFERRED, BINDS over everything below).** R1, R5, R6 were ALL item-cloze-specific → they DEFER with the item-cloze slice and are OUT of Slice 3. R4 REVERTS: only `checkDialogueClozes` relocates in Slice 3 (`checkClozeContextsFile`/`checkClozeCoverage` validate the item `cloze-contexts.ts`, which stays). R2 and R3 (dialogue-cloze idempotency + translation triangle) remain BINDING — they are (A)-path. The R1/R5/R6 text is retained below, struck through, as the spec for the future item-cloze slice.

The architect + data-architect round-1 reviews caught four issues the first draft got wrong. These corrections BIND (as amended by the re-scope banner):

- **R1 — DEFERRED to the item-cloze slice (was CRITICAL — data-arch C-1): `item_contexts` has NO unique constraint for the upsert key.** `migration.sql:148-159` has indexes on `(source_lesson_id)` and `(learning_item_id, is_anchor_context)` but **no `UNIQUE(learning_item_id, source_text)`** — so the existing `upsertClozeContext`/`upsertItemAnchorContext` `onConflict: 'learning_item_id,source_text'` would throw Postgres `42P10`. Slice 3 **must add** (idempotent DDL): `CREATE UNIQUE INDEX IF NOT EXISTS item_contexts_item_source_text_unique ON indonesian.item_contexts(learning_item_id, source_text);` + run `make migrate-idempotent-check`. The first draft's "No DDL" claim is WRONG — see corrected Supabase Requirements.
- **R2 (MAJOR — A1 + M-1): the dialogue cloze writer is NOT skip-if-exists.** `replaceDialogueClozes` (`adapter.ts:303-372`) is unconditional delete-then-insert — contradicts the ADR-0011 "reviewed clozes preserved" gate. **Resolution (binds Task 5):** the **per-dialogue-line generation gate is the sole idempotency mechanism** — for an already-seeded dialogue line (a `dialogue_clozes` row exists for its cap, keyed 1:1 by `source_line_ref`), the stage runs **neither the generator nor the writer** (no LLM call, no DB touch). This preserves L6/L9's reviewed clozes by not touching them. `replaceDialogueClozes` is invoked **only** for un-seeded lines or under `--regenerate`. A test asserts a second publish mutates zero existing `dialogue_clozes` rows. *(The `upsertClozeContext`/item_contexts half of the original R2 DEFERS with the item-cloze slice — it is not exercised in Slice 3.)*
- **R3 (MAJOR — M-2): close the `dialogue_clozes` translation triangle.** Reader `byKind/dialogueLine.ts:62-68` SELECTs `translation_text` (NOT NULL, single-language). PR 6 added nullable `translation_nl`/`translation_en` (`migration.sql:2809-2816`), currently unpopulated. **Resolution (binds Task 4):** the in-stage writer populates **`translation_text` (the NL leg, matching the PR-2 convention) AND `translation_nl` + `translation_en`** — sourced from `lesson_dialogue_lines.translation_nl/en` (PR 6, `migration.sql:2795-2801`), no extra LLM translation needed. The reader switch to `translation_nl/en` with `userLanguage` branching is **out of scope** (named for a later slice); Slice 3 keeps the reader on `translation_text` and guarantees it is populated.
- **R4 — REVISED 2026-06-03 (item cloze deferred): relocate ONE cloze check, not three.** `lint-staging.ts:951-955` runs `checkClozeContextsFile` (:517/:953), `checkClozeCoverage` (:550/:954), `checkDialogueClozes` (:596/:955). R4's original "all three" was premised on Slice 3 removing `cloze-contexts.ts` (which would make the first two error on an absent file). With the item path deferred, **`cloze-contexts.ts` stays**, so `checkClozeContextsFile` + `checkClozeCoverage` (both validate that item file) **STAY in lint-staging** and defer with the item-cloze slice. **Slice 3 relocates ONLY `checkDialogueClozes`** (`:596`) — the #126 keystone (OQ3-4) — into the DB-state-aware Capability Gate. ⚠️ **OPEN RISK — RESOLVE AT DESK BEFORE TASK 8 (architect MAJOR-1, 2026-06-03):** OQ3-4 names `checkDialogueClozes` as THE blocker for L5/7/8, but all three cloze checks feed ONE shared blocking aggregate (`lint-staging.ts:951-955,966-970`; `publish-approved-content.ts:67-72` blocks on `lint.status !== 0`), and `checkClozeCoverage` (`:550`, `word`/`phrase` items) + `checkDialogueClozes` (`:596`, `dialogue_chunk`) are wholly independent paths. **This is determinable NOW without the live DB** — at build-start run `bun scripts/lint-staging.ts 5` and `... 7`, partition the CRITICAL findings by `rule` prefix (`cloze-coverage-*`/`context-not-embedded`/`missing-*` = the staying item checks, vs the dialogue-cloze rules), and confirm the L5/7/8 block is **dialogue-only** BEFORE building the Task 8 cutover. If a non-dialogue cloze check independently fires CRITICAL on L5/7/8, #126 is only half-dissolved — escalate then (relocate that check too, or fix the exemption). Do NOT defer this to the Task 10 live trial (discovering it after the whole cutover is built is the expensive path).
- **R5 — DEFERRED to the item-cloze slice (was round-2 architect WARNING): specify the item-cloze "seeded unit" predicate.** `item_contexts` is **many-rows-per-item** (key `(learning_item_id, source_text)`, no per-item uniqueness), and the LLM-authored carrier `source_text` is **non-deterministic** — so a pre-generation gate keyed on `(learning_item_id, source_text)` can't match before generating (defeating "skip the LLM call"), while keying on item-id-has-any-cloze freezes an item at its first carrier sentence forever. **Decision for Task 5:** the item-cloze generation unit is **the item**, and the design intends a **fixed target count of carrier sentences per item** (e.g. N=the count today); the gate is "item has ≥ N `item_contexts(cloze)` rows → seeded → skip generation." State N explicitly and make the gate count-based, not content-match-based. (Dialogue mode is unaffected — its unit is the dialogue line, keyed 1:1 by `source_line_ref`.)
- **R6 — DEFERRED to the item-cloze slice (was round-2 data-arch MINOR — operator pre-apply step): dedup-check before the R1 unique index.** *(Pre-run 2026-06-03 anyway: the check returned 0 duplicate rows on the live DB, so when the item-cloze slice adds the R1 index it will not need a dedup pass — recorded here so that result isn't lost.)* `CREATE UNIQUE INDEX … item_contexts(learning_item_id, source_text)` will fail at `make migrate` if the live DB already holds duplicate `(learning_item_id, source_text)` pairs. Before applying: run `SELECT learning_item_id, source_text, count(*) FROM indonesian.item_contexts GROUP BY 1,2 HAVING count(*) > 1;` — if non-empty, dedup first. Recorded in the live-DB grounding section as a required pre-DDL step (cannot be confirmed at authoring — openbrain unavailable).

---

## Grounding (CLAUDE.md mandate — verify at execution; DB/code drift)

- **Target architecture:** `scripts/lib/pipeline/` is LOCKED (Plate IV); `src/lib/exercise-content/` is a LOCKED runtime module — its `byKind`/`byType` readers were already moved off `capability_artifacts`/`exercise_variants` in data-model PRs 1-4 and are **not changed** here (readers stay; only the stage's writers/sources change). No fold-target drift: this lands at the existing capability-stage seam, not a legacy file.
- **CONTEXT.md → Stage Contract (line 115):** explicitly forbids the Capability Stage reaching to disk staging files and names `cloze-contexts.ts`. This slice is the operational closure of that contract for these two kinds (the global closure is Slice 5 — OQ3-6).
- **ADR 0011/0012/0013:** capability content DB-authoritative after seeding (idempotent); Lesson Stage owns learner-facing content incl. translations; the gate is DB-state-aware and decomposed off `lint-staging`.

### Live-DB state — CONFIRMED 2026-06-03 (openbrain live from the slice-3 worktree session)
- Q3 ready-cap sanity check (the OQ3-3 off-ramp) — RAN:
  `select source_kind, readiness_status, publication_status, count(*) from indonesian.learning_capabilities where capability_type in ('contextual_cloze','cloze_mcq') group by 1,2,3,4 order by 1;`
  RESULT: **one row** — `dialogue_line / contextual_cloze / ready / published / 43`. **ZERO item-sourced contextual_cloze caps; ZERO cloze_mcq-typed caps.** Full item-cap matrix = {text_recognition, meaning_recall, form_recall, l1_to_id_choice, audio_recognition, dictation} only. → **OFF-RAMP TRIGGERED → DEFER item cloze (OQ3-3).**
- Baselines — RAN: `item_contexts(cloze)` = **1,167** across **699** items (orphaned — no cap); `dialogue_clozes` = **41**. ⚠️ 41 dialogue_clozes vs 43 dialogue_line caps = a **2-cap coverage gap** to confirm at build (HC15 / Task 8).
- Pre-DDL dedup check — RAN (for the future item-cloze slice's R1 index): **0 duplicate `(learning_item_id, source_text)` rows.** Clean; no dedup pass needed when R1 lands later.

### Code seams Slice 3 moves (REVISED 2026-06-03 — item cloze deferred)
- `loader.ts:141` `morphology-patterns.ts` read → **STAYS (OPTION A)** — still feeds affixed CAP emission via the regeneration; removed in Slice 5. Only the affixed ROW DATA repoints to the DB. `loader.ts:135` `cloze-contexts.ts` read → **STAYS** (deferred item path; removed in Slice 5).
- `runner.ts:370,838` `projectCloze`/`projectDialogueArtifacts` (the **dialogue** cloze path) → repointed to `lesson_dialogue_lines` (DB) + in-stage generated clozes (Mode 2). `runner.ts:362` `projectVocab` ← `staging.clozeContexts` for the **item** path → **UNCHANGED** (deferred). *(Re-read at execution; line numbers drift.)*
- `runner.ts:288` `staging.affixedFormPairs` → mapped at `:866-871` → `projectAffixedFormPairs` → repointed to `lesson_section_affixed_pairs`. *(Re-read; drifts.)*
- `lint-staging.ts:951-955` — **only `checkDialogueClozes` (:596) relocates** to the Capability Stage gate (dissolves #126). `checkClozeContextsFile` (:517) + `checkClozeCoverage` (:550) **STAY** (validate the item `cloze-contexts.ts`; defer with the item path). See R4 + its OPEN RISK.
- `cloze-creator` agent → **STAYS** for item carrier sentences (cloze-contexts.ts); its dialogue contribution is superseded by in-stage Mode-2 generation. Full retirement moves to the item-cloze slice.

---

## Supabase Requirements

### Schema changes
- **No new tables.** `dialogue_clozes`, `affixed_form_pairs`, `lesson_section_affixed_pairs`, `lesson_dialogue_lines` all already exist (data-model PRs 2/3/6).
- **No DDL in Slice 3 (REVISED 2026-06-03).** The R1 `item_contexts` UNIQUE INDEX was item-cloze-specific — it **DEFERS** with the item-cloze slice (item path untouched here, so no `onConflict` upsert is exercised). Slice 3 touches `dialogue_clozes` (keyed `UNIQUE(capability_id)`, exists) + `affixed_form_pairs` (keyed `UNIQUE(lesson_id, source_ref)`, exists) — both already have the keys their writers need. **No `migration.sql` change.**
- **No new columns.** `pattern_source_ref` is explicitly NOT added (OQ3-5, staging-only).
- RLS / grants: **N/A** — no new tables, no DDL.

### homelab-configs changes
- PostgREST schema exposure: **N/A** (no new tables).
- Kong CORS: **N/A**.
- GoTrue: **N/A**.
- Storage buckets: **N/A**.

### Health check additions (REVISED 2026-06-03 — dialogue + affixed only)
- New `CS*` Capability-Gate codes (`model.ts`) for the relocated **dialogue** cloze check: every `dialogue_line` cap has a `dialogue_clozes` row (HC15 already exists, `check-supabase-deep.ts:862-905`); every active `affixed_form_pair` cap has an `affixed_form_pairs` row (HC17 already exists, `:946-996`). **Item-cloze coverage (the DB-state successor of `checkClozeCoverage`) DEFERS with the item-cloze slice.**
- Remove **only `checkDialogueClozes`** (`:596`) from `lint-staging.ts` (Task 8) and relocate it as a **DB-state-aware** gate (m-2: surface the same coverage-gap info, don't silently downgrade). `checkClozeContextsFile` (:517) + `checkClozeCoverage` (:550) **stay** (item path deferred) — so Slice 3 does NOT empty the `lint-staging` shell, and the epic #109 shell-deletion coordination does NOT trigger here.

---

## Tasks (TDD; mirror Slice 1/2 task shape)

> **TASK RE-SCOPE (2026-06-03 — item cloze deferred).** Tasks below are revised to (A) dialogue cloze + (B) affixed repoint. Mode-1 (item carrier sentences), the item-cloze writer/gate, R1 DDL, and R5 predicate are STRUCK from Slice 3 and move to the item-cloze slice.

**Task 1 — Enforcement tests (no-disk — OQ3-6; OPTION A 2026-06-03).** `noDiskReads.test.ts` is an allowlist + whole-file-marker scanner (`DISK_IO_ALLOWLIST`; markers), NOT a per-source-kind read assertion. Under OPTION A, Slice 3 removes **no** staging reads (both `cloze-contexts.ts` and `morphology-patterns.ts` still feed cap emission via the Slice-5 regeneration), so there is **no positive-removal assertion** (the earlier `morphology-patterns.ts`-gone `it.fails` was reverted — DONE). Therefore:
  - Rely on the existing non-allowlisted walk to auto-enforce that the NEW dialogue/affixed code (`generateClozeContexts.ts`, `projectors/dialogueCloze.ts`) is disk-free — do NOT add them to the allowlist. (The `generateClozeContexts.ts` EXPECTED entry exists + is disk-free, `existsFails:false`.)
  - `loader.ts` STAYS allowlisted (both staging reads remain). Document the Slice-5 deferral in the allowlist header; keep the skipped `globalNoFileIO` placeholder naming Slice 5.
  Commit. *(SHIPPED 8af3ab7 + Option-A revert.)*

**Task 2 — `loadFromDb` dialogue + affixed read.** Extend the import seam to read `lesson_dialogue_lines` + `lesson_section_affixed_pairs` (PR 6) + current `dialogue_clozes`/`affixed_form_pairs` cap state (the seeded-delta signal). *(No `item_contexts(cloze)` read — item path deferred.)* Paginated, disk-free. TDD with seeded fixtures. Commit.

**Task 3 — Pure projector repoint.** `projectDialogueArtifacts` + `projectAffixedFormPairs` + the **dialogue** cloze path of `projectCloze` consume DB-sourced rows (+ generated clozes), not `staging.*`. *(projectVocab's item-cloze path is UNCHANGED — deferred.)* Pure functions; deterministic. Preserve the existing cap shapes (`dialogue_line:contextual_cloze`, `affixed_form_pair:root_derived_*`), `requiredArtifacts:[]` (Decision R), canonical keys unchanged (FSRS protection — verify no learner state churn). **Repoint the dialogue cap EMISSION too:** `projectors/vocab.ts:173-209` currently derives `dialogue_line:contextual_cloze` caps from `staging.clozeContexts` (matching item slug → dialogue line) — re-derive these from the Mode-2 generator output / `lesson_dialogue_lines` instead, so the dialogue path no longer depends on `clozeContexts`. TDD. Commit.

**Task 4 — In-stage dialogue cloze generator (port the agent; ONE mode — Mode 2 only).** `generateClozeContexts.ts` mirrors `generateItemDistractors.ts`/`generateGrammarExercises.ts`: pure prompt builder (port `cloze-creator.md`'s dialogue contract), pure parser, thin Claude call, injectable `generateFn`, `ANTHROPIC_API_KEY` no-op, **defensive sanitization** to the typed-row shapes (Slice-1 Lesson #4).
  - **Mode 2 (dialogue line) — the ONLY mode in Slice 3:** blank one vocab word in the existing line → `dialogue_clozes`, AND **emit the skip decision** for ineligible lines (port the eligibility logic from `lint-staging.ts:589-646`: ≥6 tokens; blanked word is current/prior-lesson vocab; ≥2 same-POS distractors in the pool; valid `clozeSkips` reasons). Short lines ("Sudah.") → skip.
  - *(Mode 1 (item carrier sentence → `item_contexts(cloze)`) is DEFERRED to the item-cloze slice.)*
  - **Translation fields (R3 / M-2 — BINDS):** the dialogue writer populates **`translation_text` (the NL leg) AND `translation_nl` + `translation_en`**, sourced from `lesson_dialogue_lines.translation_nl/en` (PR 6) — no extra LLM translation. The reader stays on `translation_text` (its switch to `translation_nl/en` is out of scope). A test asserts `translation_text` is non-NULL on every written `dialogue_clozes` row (the reader contract). **Wire the writer shape (data-arch m-1 — REQUIRED):** `DialogueClozeInput` (`adapter.ts:288-301`) has NO `translation_nl`/`translation_en` fields and `replaceDialogueClozes` (`adapter.ts:357-368`) inserts only the existing 5 — so this task must **extend `DialogueClozeInput` with `translation_nl?: string | null` + `translation_en?: string | null`** and **extend the `replaceDialogueClozes` insert mapping** to write them. Without this the generator has nowhere to put the R3 fields.
  Unit-test the pure parts (prompt build, parse, sanitize, skip-decision, translation-field assignment). Commit.

**Task 5 — Idempotent dialogue cloze writer + per-line generation gate (R2 — REVISED; R1/R5 deferred).** No DDL (R1 deferred). 
  - **The per-dialogue-line generation gate is the SOLE idempotency mechanism** (R2): for an already-seeded line (a `dialogue_clozes` row exists for its cap, keyed 1:1 by `source_line_ref`) the stage runs **neither the generator nor the writer** — no LLM call, no DB touch. This preserves L6/L9's reviewed clozes by *not touching them*, sidestepping `replaceDialogueClozes`'s delete-insert semantics.
  - **Writer safety net:** `replaceDialogueClozes` (`adapter.ts:303-372`, delete+insert) is invoked **only** for un-seeded lines or under `--regenerate`, never on the routine re-run path.
  - `--regenerate <line>` deletes + rebuilds that line. TDD: seed twice → **second run mutates zero existing `dialogue_clozes` rows** (assert row `updated_at`/content unchanged, not just count); `--regenerate` → that line replaced, others untouched (ADR 0011). Commit.

**Task 6 — Affixed repoint writer.** `replaceAffixedFormPairs` reads `lesson_section_affixed_pairs` (root/derived/affix/allomorph_rule; `migration.sql:2723-2726`) instead of `pipelineInput.affixedFormPairs` (the current staging feed at `runner.ts:866-871`, sourced via `loader.ts:141` → `morphology-patterns.ts`); `allomorph_rule` NOT NULL passthrough (OQ3-7); `patternSourceRef` NOT persisted (OQ3-5 — annotate `CurrentAffixedFormPair.patternSourceRef` `@stagingOnly`). **Canonical-key stability (M-3):** the in-stage generator must derive the cap's `source_ref` from the DB row identically to the staging path's `affixedFormPairSourceRef(...)` so `learning_capabilities.canonical_key` is byte-identical (else learner FSRS state orphans) — `lesson_section_affixed_pairs` is keyed `UNIQUE(lesson_id, source_ref)` (`migration.sql:2729`). **Spell out the assertion (data-arch m-2):** for the trial lesson, query `lesson_section_affixed_pairs.source_ref` for every row and compare against `affixedFormPairSourceRef(lessonNumber, pair)` (`content-pipeline-output.ts:148-150`, incl. its `stableSlug(root-derived)` fallback when `pair.sourceRef` is falsy) — **fail the task if ANY pair diverges** (a divergence = canonical_key drift = orphaned FSRS state). All current staging entries carry explicit `sourceRef` (`morphology-patterns.ts:4,12`) so live data passes; the assertion guards future `sourceRef`-less entries. Stop reading `morphology-patterns.ts`. TDD. Commit.

**Task 7 — Runner cutover.** Wire `loadFromDb` dialogue/affixed read → repointed projectors → dialogue cloze generator (gated) → idempotent dialogue writer. **Remove the dialogue `staging.clozeContexts` feeds** (`runner.ts:370` projectCloze, `:838` projectDialogueArtifacts) **and the `staging.affixedFormPairs` feed** (`runner.ts:288` → mapped at `:866-871`). **LEAVE `runner.ts:362` projectVocab's `staging.clozeContexts` feed intact** (the deferred item-cloze path). *(Re-read the runner before editing — line numbers drift; the first draft's cites at :286/:826/:831 were imprecise.)* **No double-write** (Slice-1 Task-6c lesson): the dialogue/affixed caps written by exactly one path; filter from the legacy `staging.capabilities` bundle by the exact canonical_keys the new path emits (NOT by `sourceKind` alone). Retire `capability_artifacts` writes/reads for the dialogue + affixed kinds. TDD + no-double-write assertion. Commit.

**Task 8 — Capability Gate layer + RELOCATE `checkDialogueClozes` (OQ3-4 — dissolve #126; R4 REVISED).** New post-write, DB-state-aware validators (dialogue_clozes coverage, affixed coverage). New `CS*` codes in `model.ts`. **Wire into the runner's post-write gate** (Slice-1/2 Task-7 lesson: validators are dead unless the runner feeds them). **Surgically remove ONLY `checkDialogueClozes` (`:596`) from `lint-staging.ts`** — relocate its eligibility/skip logic into the in-stage generator (Task 4) + the gate. `checkClozeContextsFile` (:517) + `checkClozeCoverage` (:550) **STAY** (item path deferred); Slice 3 does NOT empty the `lint-staging` shell, so the epic #109 shell-deletion coordination does NOT trigger. **Semantics shift (m-2):** the old check was a staging-file check; the new gate is DB-state-aware — confirm it surfaces the same dialogue coverage-gap information (don't silently downgrade). **⚠️ OPEN RISK (R4):** if the L5/L7 live trial shows `checkClozeCoverage`/`checkClozeContextsFile` *also* block (not just `checkDialogueClozes`), #126 is only half-dissolved — escalate (relocate that check too, or fix its exemption). Do not paper over. Commit.

**Task 9 — DEFERRED TO SLICE 5 (OPTION A 2026-06-03; documentation-only here).** The `morphology-patterns.ts` read CANNOT be removed in Slice 3: the affixed CAP emission flows through the staging-derived `buildCapabilityStagingFromContent` regeneration (`runner.ts` → `projectCapabilities` ← `staging.affixedFormPairs` ← `morphology-patterns.ts`), and that regeneration is retired in Slice 5. Removing the read now would emit zero affixed caps → soft-retired. Same entanglement as `cloze-contexts.ts`. So BOTH residual staging reads + `cloze-creator` retirement move to **Slice 5** alongside the regeneration teardown. No Slice 3 code change.

**Task 10 — Integration + MANDATORY live trial (on a #126-blocked lesson).** Canonical integration test (seed dialogue+affixed fixtures → run → assert `dialogue_clozes`/`affixed_form_pairs` rows → re-run idempotent → `--regenerate` replaces one line). **Then the live trial: publish L5 or L7** (a #126-blocked lesson — LIVE-VERIFY its `lesson_dialogue_lines` exist from a PR-6 re-publish first; if lint-blocked from publishing lesson content, that confirms the gate-relocation is the unblock). Verify: dialogue clozes generated for its dialogue lines (**#126 dissolved — no manual cloze-creator run for the dialogue path**); the curated distractors + grammar cutover land in the **same** publish (the #126 keystone payoff); a **dialogue cloze renders**; a `capability_review_events` row lands. **Confirm the R4 OPEN RISK** (did any non-dialogue cloze check block the publish?). Re-run the OQ3-3 ready-cap check (should still show item cloze absent — deferral correct). For the coverage gap: confirm the trial lesson's own dialogue caps each get a `dialogue_clozes` row (HC15 green for THAT lesson) — the global 41-vs-43 gap closes only when the lesson(s) owning the 2 missing rows re-publish, so log which lesson owns them rather than expecting a single-lesson trial to close a global count (architect MINOR-1). Sweep for the 4 Slice-1 bug classes. Fix anything surfaced.

---

## Gates (before merge — REVISED 2026-06-03, dialogue + affixed only)
- [ ] Enforcement: the NEW dialogue/affixed code is disk-free (non-allowlisted walk); `loader.ts` stays allowlisted (OPTION A — both staging reads defer to Slice 5); skipped `globalNoFileIO` placeholder names Slice 5. *(No read removed in Slice 3.)*
- [ ] `dialogue_clozes` / `affixed_form_pairs` written by the stage from DB sources; `capability_artifacts` no longer written/read for the dialogue + affixed kinds. *(item_contexts(cloze) untouched — deferred.)*
- [ ] Idempotency: re-run writes nothing new; `--regenerate <line>` replaces only that dialogue line (L6/L9 reviewed clozes preserved).
- [ ] `checkDialogueClozes` relocated off `lint-staging` into the Capability Gate; #126-blocked lesson now publishes. **R4 OPEN RISK closed:** confirm no other cloze check independently blocked the trial lesson.
- [ ] `patternSourceRef` annotated `@stagingOnly`; no `pattern_source_ref` column.
- [ ] `bun run test` + `bun run lint` + `bun run build` green; `make pre-deploy` (pre-existing failures only).
- [ ] **Live trial passed on a #126-blocked lesson** — dialogue cloze renders, `capability_review_events` lands, curated distractors + grammar cutover delivered in the same publish; OQ3-3 ready-cap check still shows item cloze absent (deferral correct).
- [ ] Dual re-review (architect + data-architect) of this re-scoped plan recorded in `reviewed_by`; status → approved.

## Out of scope (later slices)
- **Item-cloze slice (NEW 2026-06-03) — build the deferred feature:** emit `item:contextual_cloze` caps (the `runner.ts:701-703` hook), port the Mode-1 carrier-sentence generator (extends Slice 3's Mode-2 infra), write `item_contexts(cloze)` from DB-sourced items, wire `cloze_mcq_item_distractors`, add R1 (item_contexts UNIQUE index) + R5 (count-based seeded predicate) + R6 (dedup — pre-confirmed clean 2026-06-03), then remove the `cloze-contexts.ts` read + relocate `checkClozeCoverage`/`checkClozeContextsFile` + retire `cloze-creator`. **A feature build with a product question (do we want item-level cloze as a live exercise?), not a migration.** The 1,167 existing `item_contexts(cloze)` rows + 699 items are preserved untouched until then.
- **Slice 4 (#102) — teardown:** drop `capability_artifacts` / `exercise_variants` + the safe-set, per the 2026-05-25 pre-drop census (incl. the `exercise_review_comments` ON DELETE CASCADE risk + the still-present `leaderboard` view). Slice 3 stops *writing* the artifacts for the dialogue + affixed kinds; it does not drop tables.
- **Slice 5 (legacy-projection retirement) — NEW (OQ3-6):** retire the residual staging projection (audio/sentence item caps from `learning-items.ts`, legacy grammar from `grammar-patterns.ts`/`candidates.ts`, the `content-units.ts`/`capabilities.ts`/`exercise-assets.ts` derivation + `stagingWriteback`). This is where the GLOBAL no-file-I/O enforcement gate (epic #98 User Story 10) flips on. Without Slice 5 the epic's no-disk goal never fully lands.
- **Module spec updates:** any touched module spec (`docs/current-system/modules/exercise-content.md` if a reader contract shifts) updated same-commit (CLAUDE.md).
