# Agent 7: Authoring scripts, OCR, content data

**Date:** 2026-05-20
**Files reviewed:** ~45 (scripts/*.ts owned by this slice, scripts/data/, scripts/fixtures/, scripts/spoken-variant-generator/, tools/review/, Makefile cross-checks)

## Files reviewed

`scripts/` (owned slice only):
- `add-localhost-redirect.ts`, `ocr-pages.ts`, `parse-lesson-content.ts`, `fetch-lesson-content.ts`, `catalog-lesson-sections.ts`, `generate-staging-files.ts`, `generate-exercises.ts`, `generate-exercise-audio.ts`, `generate-section-audio.ts`, `convert-heic-to-jpg.ts`, `build-sections.ts`, `set-lesson-voices.ts`, `lint-staging.ts` (skimmed; large), `reverse-engineer-staging.ts`
- `seed-lessons.ts`, `seed-podcasts.ts`, `seed-lesson-audio.ts`, `seed-learning-items.ts`, `seed-cloze-contexts.ts`, `seed-lesson4-grammar-exercises.ts`, `seed-drying-scenario.ts`
- `repair-item-meanings.ts`, `repair-stability.ts`, `reactivate-dialogue-chunks.ts`, `cleanup-annotations.ts`
- `backfill-pos.ts`, `extract-cloze-items.ts`, `fill-english-translations.ts`, `audit-translation-parens.ts`, `check-vocab-coverage.ts`, `check-pos-coverage.ts`, `check-lesson-coverage.ts`, `check-exercise-coverage.ts`, `check-section-content.ts`, `check-admin.ts`, `check-viewport-math.sh`
- `test-grammar-exercises.mjs`, `test-e2e-pipeline.ts`
- `scripts/data/lessons.ts`, `vocabulary.ts`, `lesson-4.ts`, `podcasts.ts`
- `scripts/data/staging/lesson-{1..10}/` (headers + size sampling), `staging/podcast-warung-market/`
- `scripts/fixtures/*`, `scripts/spoken-variant-generator/` (file inventory)
- `tools/review/`, `Makefile`

## Findings

### F7-1: `scripts/add-localhost-redirect.ts` is a no-op stub
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/add-localhost-redirect.ts:1` — "Add localhost:5173 to Supabase auth redirect URLs"
  - `scripts/add-localhost-redirect.ts:21-46` — Selects `auth.config`, then prints "⚠️ Redirect URL updates via API require Supabase management endpoints / which may not be available in self-hosted instances" and asks the operator to do it manually in Studio.
- **Recommendation:** Delete. The script admits it cannot perform its stated task and produces only a hand-instruction printout. Self-host redirects are a one-time Studio config — no script needed.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-2: `scripts/seed-lesson4-grammar-exercises.ts` is a half-finished, superseded one-off
- **Severity:** cleanup
- **Category:** half-finished-migration
- **Evidence:**
  - `scripts/seed-lesson4-grammar-exercises.ts:88-92` — "Approved candidates would be seeded here. exercise_variants requires learning_item_id + context_id FKs — approved candidates must carry those before this block runs. **Not implemented yet.**"
  - `scripts/seed-lesson4-grammar-exercises.ts:91` — `console.log(\`\n  Would seed ${approvedCandidates.length} approved candidates (not yet implemented).\`)`
  - Lesson 4 has shipped via the full capability pipeline (`scripts/data/staging/lesson-4/{capabilities.ts,content-units.ts,exercise-assets.ts,lesson-page-blocks.ts}` are all "Regenerated by capability-stage runner").
- **Recommendation:** Delete the script. The pipeline (`bun scripts/publish-approved-content.ts 4`) is the supported path and is what published lesson 4. The script writes a stale grammar_patterns shape (`pattern_name` → `name`, `short_explanation`, `complexity_score`) that may conflict if re-run.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-3: `seed-learning-items.ts` + `scripts/data/vocabulary.ts` are runtime-orphan legacy
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `CLAUDE.md` — "The `vocabulary` table is not read at runtime."
  - `docs/plans/2026-05-08-pipeline-cleanup-for-lessons-fold.md:941` — "Legacy infrastructure (... `seed-learning-items.ts`, `seed-cloze-contexts.ts`, `repair-item-meanings.ts` scripts; `scripts/data/vocabulary.ts`) retires."
  - `scripts/data/vocabulary.ts` covers `lesson_order_index` 1–4 (61 + 65 + 62 + 83 entries); lessons 4+ are pipeline-authored, so the L4 entries are display-only (per `scripts/seed-lessons.ts:60-76` warning loop).
  - `scripts/seed-learning-items.ts:92-149` writes directly to `item_meanings`, `item_answer_variants`, `item_contexts` — the capability pipeline owns these.
  - Makefile `seed-all` (Makefile:126) still chains `seed-learning-items` + `seed-sentences` (which actually delegates to `extract-cloze-items.ts`, not a real seed-sentences script — Makefile:114-117).
- **Recommendation:** Delete `scripts/seed-learning-items.ts` and `scripts/data/vocabulary.ts`. Drop the `seed-learning-items` and `seed-sentences` Makefile targets and adjust `seed-all` to only chain the still-living seeders.
- **Estimated effort:** small
- **Cross-slice dependency:** null

### F7-4: `scripts/extract-cloze-items.ts` writes orphan cloze items, has zero relationship to the capability cloze pipeline
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/extract-cloze-items.ts:24-251` — hard-coded `clozeItems` table with `lessonNumber: 1..3` only.
  - Today's cloze contexts are seeded via `scripts/seed-cloze-contexts.ts` (read from `scripts/data/staging/lesson-N/cloze-contexts.ts`), and live capability cloze flows through capability-stage projectors.
  - Makefile (`Makefile:114-117`) wires `seed-sentences` → `extract-cloze-items.ts` — but the script's hard-coded data covers only lessons 1–3.
- **Recommendation:** Delete `scripts/extract-cloze-items.ts` and remove `seed-sentences` from the Makefile.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-5: `scripts/repair-stability.ts` operates on legacy-retained tables that no longer receive writes
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/repair-stability.ts:1-11` — "Repairs `learner_skill_state.stability` values that were damaged by the grammar adjustment bug" — one-shot from the FSRS-pre-capability era.
  - `docs/current-system/data-model.md:180-182` — `learner_item_state`, `learner_skill_state`, `review_events` are legacy-retained, **no new rows**. Live FSRS state is in `learner_capability_state`.
  - The script replays through ts-fsrs with hard-coded weights at `scripts/repair-stability.ts:38-41` — those weights belong to a prior era and aren't synchronised with anything live.
- **Recommendation:** Delete. Schedule removal of `learner_skill_state` & `review_events` separately (data-model.md notes "follow-up retirement is cheap").
- **Estimated effort:** trivial
- **Cross-slice dependency:** agent 8 (if the tables are dropped, deep-check needs update)

### F7-6: `parse-lesson-content.ts` is superseded by `catalog-lesson-sections.ts` + `generate-staging-files.ts` but still wired in the Makefile
- **Severity:** cleanup
- **Category:** half-finished-migration
- **Evidence:**
  - `docs/process/content-pipeline.md:59-60` — Step 3 is `catalog-lesson-sections.ts`, step 4 is `generate-staging-files.ts`. There is no `parse-lesson-content.ts` step in the canonical authoring flow.
  - `scripts/parse-lesson-content.ts:1-13` — claims to be "Step 3 of content pipeline" (the old shape).
  - `Makefile:143-146` still exposes `make parse-lesson LESSON=<N>`, and `make pipeline` chains it via `parse-lesson` (Makefile:152-154).
  - The regex parser produces a sections shape (`{ type: 'text', paragraphs: [...] }`) and a wholly different sectioning model than `catalog-lesson-sections.ts`'s LLM output (`{ type, items, lines, source_pages, confidence, ... }`).
- **Recommendation:** Delete `parse-lesson-content.ts` and the `parse-lesson` + `pipeline` Makefile targets. If the LLM-free fallback is wanted, add a flag to `catalog-lesson-sections.ts` instead of keeping two divergent parsers.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-7: `scripts/data/lesson-4.ts` is a pre-pipeline draft, never imported anywhere
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/data/lesson-4.ts:1-2` — "Lesson 4: Di Hotel ... Auto-extracted from coursebook pages. Review and correct before committing."
  - `grep -rn "lesson-4\\.ts\\|from.*data/lesson-4"` across the codebase returns no consumers.
  - The lesson is now authoritative in `scripts/data/staging/lesson-4/lesson.ts` (and was published).
- **Recommendation:** Delete `scripts/data/lesson-4.ts`.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-8: `scripts/data/staging/lesson-4/pages.ts` is an orphan referencing a missing type and missing extractor
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/data/staging/lesson-4/pages.ts:1-4` — "Auto-generated by extract-textbook-content.ts / Do not edit manually / import type { TextbookPage } from '@/types/contentGeneration'".
  - `ls src/types/contentGeneration*` — no such file exists.
  - `grep -rn extract-textbook-content scripts/` — no such script exists in the repo.
  - The file is `~21 KB` of stringified OCR for lesson 4 only. No imports from anywhere.
- **Recommendation:** Delete `scripts/data/staging/lesson-4/pages.ts`. (And, since other lessons use `sections-catalog.json` instead, this is purely vestigial.)
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-9: `scripts/test-grammar-exercises.mjs` is an orphan Playwright smoke script
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/test-grammar-exercises.mjs:1` — `import { chromium } from 'playwright-core'`, hits `https://indonesian.duin.home` directly.
  - `grep -rn "test-grammar-exercises"` outside the file itself returns zero hits — no Makefile, package.json, CI, or docs reference.
  - The `.mjs` extension stands out — the rest of the suite uses bun + TS.
- **Recommendation:** Either delete or move to `scripts/__tests__/` with the rest of the suite. Today it's a one-off run-by-hand script that has likely outlived its purpose.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-10: `scripts/test-e2e-pipeline.ts` is an orphan, runs no real assertions
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/test-e2e-pipeline.ts:1-10` — claims "End-to-end test of the content pipeline" but only checks that feature-flag toggles and mock arrays read out as expected (`scripts/test-e2e-pipeline.ts:30-54`).
  - `grep -rn "test-e2e-pipeline"` returns no callers — not wired into Makefile, vitest, CI, or docs.
  - Imports `featureFlags` and `ExerciseType` from `src/` (`scripts/test-e2e-pipeline.ts:12-13`) — direct coupling of a one-off script to runtime code.
- **Recommendation:** Delete.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-11: `scripts/check-viewport-math.sh` has no caller despite being a seam-contract guard
- **Severity:** cleanup
- **Category:** test-gap
- **Evidence:**
  - `scripts/check-viewport-math.sh:1-5` — "Enforces the page framework seam contract … See docs/plans/2026-04-24-page-framework-design.md §4.3."
  - `grep -rn check-viewport-math` returns only the file itself — no `Makefile`, `package.json`, CI, or pre-commit hook reference.
  - CLAUDE.md (admin-design-surfaces section) lists the page-framework smoke test on `/admin/page-lab` — but the static-CSS guard here is not invoked anywhere.
- **Recommendation:** Either wire it into `make lint` / pre-deploy or delete it. Right now the seam contract has no automated enforcement despite this guard existing.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-12: Makefile `seed-flashcards` target references a non-existent script
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `Makefile:104-107` — `.PHONY: seed-flashcards` → `bun scripts/seed-flashcards.ts`.
  - `ls scripts/seed-*` — no `seed-flashcards.ts` exists.
- **Recommendation:** Delete the `seed-flashcards` Makefile target.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-13: Makefile `seed-sentences` target is mis-wired
- **Severity:** cleanup
- **Category:** inconsistency
- **Evidence:**
  - `Makefile:114-117` — declares `seed-sentences` target, but invokes `bun scripts/extract-cloze-items.ts`. The target name doesn't match what it runs, and `extract-cloze-items.ts` is itself dead (F7-4).
- **Recommendation:** Drop the target as part of F7-4 cleanup.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-14: `scripts/data/staging/lesson-10/index.ts` exports four files that do not exist
- **Severity:** bug
- **Category:** bug
- **Evidence:**
  - `scripts/data/staging/lesson-10/index.ts:6-9` — `export { contentUnits } from './content-units'` / `capabilities` / `lessonPageBlocks` / `exerciseAssets`.
  - `ls scripts/data/staging/lesson-10/` — only `candidates.ts cloze-contexts.ts grammar-patterns.ts index.ts learning-items.ts lesson.ts sections-catalog.json` exist; none of the four derived files do.
  - Lesson 10 is in pre-publish state — `generate-staging-files.ts` wrote the `index.ts` (per `generateIndexTs` at `scripts/generate-staging-files.ts:347-359`) but the capability-stage runner hasn't projected the derived files yet.
- **Recommendation:** Make `generate-staging-files.ts:347-359` emit the workflow exports conditionally (only after the capability-stage runner produces them), or scaffold empty stub files at the same point grammar-patterns/candidates/cloze-contexts are scaffolded (`generate-staging-files.ts:472-474`). Today, any consumer that does `import { capabilities } from './lesson-10'` blows up with an unresolved-module error pre-publish.
- **Estimated effort:** trivial
- **Cross-slice dependency:** agent 6 (capability-stage runner owns the regeneration)

### F7-15: Reverse-engineered legacy catalog format is silently inconsistent with the canonical one
- **Severity:** cleanup
- **Category:** inconsistency
- **Evidence:**
  - `scripts/reverse-engineer-staging.ts:263-269` writes catalogs with `lesson_number`, `lesson_title`, `level`, `sections[].section_type` (lesson-1 example confirms this).
  - `scripts/generate-staging-files.ts:144-161` (`canUseCatalogForGeneration`) requires the **new** shape (`lessonMeta`, `sections[].type`), and silently falls back when the reverse-engineered shape is present: `scripts/generate-staging-files.ts:439-441` — "Using existing curated staging files because sections-catalog.json is in the legacy reverse-engineered format."
  - Net effect: lessons 1-3 carry a `sections-catalog.json` that **cannot** be used to regenerate `lesson.ts` if the curated staging files are lost. The format mismatch is undocumented in either script.
- **Recommendation:** Either (a) make `reverse-engineer-staging.ts` emit the canonical shape (rename `section_type` → `type`, wrap meta in `lessonMeta`, add `generatedAt`/`sourcePages`/`flags`), or (b) delete the script — lessons 1-3 are settled and the catalog files are no longer load-bearing.
- **Estimated effort:** small
- **Cross-slice dependency:** null

### F7-16: `seed-cloze-contexts.ts`, `repair-item-meanings.ts`, `reactivate-dialogue-chunks.ts`, `cleanup-annotations.ts` write to capability-stage-owned tables outside the runner
- **Severity:** nice-to-have
- **Category:** architecture-violation
- **Subtype:** pipeline-shortcut
- **Evidence:**
  - `scripts/seed-cloze-contexts.ts:101-114` — direct upsert to `item_contexts`.
  - `scripts/repair-item-meanings.ts:77-87` — `delete` + `insert` against `item_meanings`.
  - `scripts/reactivate-dialogue-chunks.ts:167-170` — `update({ is_active: true })` on `learning_items`.
  - `scripts/cleanup-annotations.ts:101-122` — updates `learning_items.base_text`/`normalized_text` and deletes from `audio_clips`.
  - These tables are owned by `runCapabilityStage` (CLAUDE.md "Data Model Overview" — "Pipeline" writes to `item_meanings`, `item_contexts`, etc.). Repair scripts bypass the runner and can silently desync content-unit / capability-artifact projections.
- **Recommendation:** Inventory which of these are still genuinely needed after the deterministic-snapshot-regen plan ships. `repair-item-meanings.ts` and `cleanup-annotations.ts` should arguably re-trigger `runCapabilityStage` for affected lessons rather than mutate child rows directly. The plan at `docs/plans/2026-05-08-pipeline-cleanup-for-lessons-fold.md:941` already targets these for retirement.
- **Estimated effort:** medium
- **Cross-slice dependency:** agent 6 (capability-stage)

### F7-17: `VALID_POS` is duplicated between `catalog-lesson-sections.ts` and the shared module
- **Severity:** cleanup
- **Category:** duplication
- **Evidence:**
  - `scripts/lib/validate-pos.ts:4` — exports `VALID_POS` as a `Set` (shared module, used by `backfill-pos.ts:22`).
  - `scripts/catalog-lesson-sections.ts:40-45` — redefines `VALID_POS` as an `as const` array plus its own derived `POS` type.
  - Both list the same 12 values but drift would only be caught at runtime in one of the two.
- **Recommendation:** Have `catalog-lesson-sections.ts` import from `scripts/lib/validate-pos.ts` (export an array form or derive the type from the Set).
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-18: Five LLM-using authoring scripts pin three different Claude model strings; one is hardcoded with a release suffix
- **Severity:** nice-to-have
- **Category:** inconsistency
- **Evidence:**
  - `scripts/build-sections.ts:129, 163` — `'claude-sonnet-4-6'`
  - `scripts/generate-exercises.ts:146` — `'claude-sonnet-4-6'`
  - `scripts/catalog-lesson-sections.ts:376` — `'claude-sonnet-4-6'`
  - `scripts/backfill-pos.ts:51` — `MODEL = 'claude-sonnet-4-6'`
  - `scripts/fill-english-translations.ts:30` — `'claude-haiku-4-5-20251001'` (suffix-pinned)
  - Pipeline lib files use mixed strings: `scripts/lib/pipeline/lesson-stage/enrichGrammarTopics.ts:35` = `'claude-haiku-4-5-20251001'`, `scripts/lib/pipeline/capability-stage/enrichPos.ts:37` = `'claude-sonnet-4-6'`.
- **Recommendation:** Hoist a single `MODEL_SONNET` / `MODEL_HAIKU` constant (e.g. `scripts/lib/anthropic-models.ts`) and import everywhere. Today these will drift independently as models update; the suffix-pinned variant is especially silent — it ages out without anyone noticing.
- **Estimated effort:** small
- **Cross-slice dependency:** agent 6 (lib pipeline files use them too)

### F7-19: `seed-lessons.ts` still gates lessons 4+ with a vocabulary-display warning that's now a relic
- **Severity:** nice-to-have
- **Category:** spec-drift
- **Evidence:**
  - `scripts/seed-lessons.ts:60-84` warns "has vocabulary sections but no staging file — vocabulary will NOT be schedulable" for each lesson 4+ in `scripts/data/lessons.ts`.
  - `scripts/data/lessons.ts:1-3` is explicitly labelled "LEGACY FILE — lessons 1–3 only."
  - Today lessons 4+ are pipeline-authored — they live in `scripts/data/staging/lesson-N/` and are published with `publish-approved-content.ts`. `lessons.ts` should not contain lesson 4+ entries; if it does, the warning fires but the operator is then told to run a script (`publish-approved-content.ts`) that doesn't read from `lessons.ts` anyway.
- **Recommendation:** Trim `scripts/data/lessons.ts` to lessons 1–3 only (its declared scope), and either drop the pipeline-warning loop in `seed-lessons.ts` or convert it to a hard fail. Today the warning is informational noise.
- **Estimated effort:** small
- **Cross-slice dependency:** null

### F7-20: `tools/review/` still lives in the repo despite the linguist-* agent flow being the documented authoring path
- **Severity:** nice-to-have
- **Category:** spec-drift
- **Evidence:**
  - `Makefile:148-150` — `make review` → `cd tools/review && bun run dev`.
  - `tools/review/server.ts:1-5` — Express server "reads/writes OCR text and staging files. Also serves page images from content/raw/."
  - `docs/process/content-pipeline.md:59-66` describes the 8-step flow as `catalog-lesson-sections` → `generate-staging-files` → `linguist-structurer` → `linguist-reviewer` agents. The review UI is not mentioned in that doc as a step.
  - `tools/review/node_modules/` is checked-in/sized as a directory — the working tree has 178 `node_modules` entries (`ls tools/review/node_modules | wc -l`).
- **Recommendation:** Either re-establish `tools/review/` in the canonical pipeline (add it to `content-pipeline.md`) or retire it. Either way, `tools/review/node_modules` should not be present — add `tools/review/node_modules` to `.gitignore` (verify it isn't already by checking git tracking) and ensure `tools/review/tsconfig.tsbuildinfo`, `.DS_Store`, etc. aren't tracked.
- **Estimated effort:** small (retire) or small (re-document)
- **Cross-slice dependency:** null

### F7-21: `scripts/fixtures/` has fixture files no tests import
- **Severity:** cleanup
- **Category:** dead-code
- **Evidence:**
  - `scripts/fixtures/sample-candidates.ts:2` — "fixture representing existing candidates in various states"
  - `scripts/fixtures/sample-lesson-stub.ts:2` — "fixture for build-sections and generate-exercises unit tests"
  - `scripts/fixtures/sample-grammar-patterns.ts:2` — "fixture for generate-exercises unit tests"
  - `grep -rn "scripts/fixtures\\|sample-candidates\\|sample-lesson-stub\\|sample-grammar-patterns"` returns only the fixture files themselves and the comments — no tests import them. The implied unit tests do not exist.
- **Recommendation:** Either write the tests these were prepared for (against `build-sections.ts` / `generate-exercises.ts`) or delete the fixture files. Today they're cargo.
- **Estimated effort:** trivial (delete) or medium (write the tests)
- **Cross-slice dependency:** null

### F7-22: `scripts/backfill-pos.ts` is a "one-shot" still in the tree after running
- **Severity:** nice-to-have
- **Category:** dead-code
- **Evidence:**
  - `scripts/backfill-pos.ts:3-4` — "One-shot backfill: tag learning_items.pos for existing word/phrase items where pos is currently NULL."
  - `scripts/check-pos-coverage.ts:25-27` is the diagnostic and queries `pos IS NULL` — the live POS pipeline is `scripts/lib/pipeline/capability-stage/enrichPos.ts` per `enrichPos.ts:37`.
  - The script writes a checkpoint file `/tmp/pos-backfill-progress.json` (line 49) — operational state that suggests it's been run.
- **Recommendation:** If POS coverage is now driven by the capability-stage `enrichPos` (Stage B enrichment), this backfill is no longer needed. Confirm coverage with `check-pos-coverage.ts` against the live DB, then delete `backfill-pos.ts`.
- **Estimated effort:** trivial
- **Cross-slice dependency:** agent 6 (verify enrichPos coverage)

### F7-23: `audit-translation-parens.ts` produces a CSV no one consumes; flags rather than fixes
- **Severity:** nice-to-have
- **Category:** dead-code
- **Evidence:**
  - `scripts/audit-translation-parens.ts:1-12` — outputs a CSV "for manual review", default `/tmp/translation-parens-review.csv`.
  - No Makefile, CI, doc, or follow-up tool references it.
- **Recommendation:** Either fold the heuristic into `lint-staging.ts` (which already gates publish) so the signal is enforced, or delete this script — a manual one-off review tool that's been run once leaves only the CSV behind.
- **Estimated effort:** small
- **Cross-slice dependency:** null

### F7-24: `fill-english-translations.ts` walks **published** lesson staging dirs and rewrites learning-items.ts; no guard against clobbering enrichment
- **Severity:** nice-to-have
- **Category:** bug
- **Evidence:**
  - `scripts/fill-english-translations.ts:100-104` — writes back `learning-items.ts` with `JSON.stringify(updated, null, 2)`, overwriting whatever shape was there.
  - The capability-stage runner regenerates `learning-items.ts` after enrichment (POS, level, EN translations, dialogue NL propagation — `generate-staging-files.ts:384-390` comment). If a user runs `fill-english-translations.ts --all` after publish, it will rewrite the file but **strip** any extra fields the pipeline added that aren't `base_text`/`translation_nl`/`translation_en` (e.g. `pos`, `level`).
  - The script's "Next" line (`scripts/fill-english-translations.ts:132`) suggests running `repair-item-meanings.ts` afterwards — implies the author knew it needed a downstream sync.
- **Recommendation:** Either preserve unknown fields when rewriting (deep-merge instead of full replace), or — since EN translations are now enriched by `enrichEnTranslations.ts` (Stage B) — retire this script.
- **Estimated effort:** small
- **Cross-slice dependency:** agent 6 (own enrichEnTranslations)

### F7-25: `set-lesson-voices.ts` voice rotation is hard-coded for 8 lessons; lesson 9 silently cycles
- **Severity:** nice-to-have
- **Category:** bug
- **Evidence:**
  - `scripts/set-lesson-voices.ts:25-34` — `VOICE_ROTATION` has 8 entries (lesson 1..8).
  - Lessons 9 and 10 staging dirs exist (`scripts/data/staging/lesson-9/`, `lesson-10/`). The rotation array indexing relies on `(lessonOrder - 1) % VOICE_ROTATION.length` semantics — but the comment freezes the mapping at lessons 1..8.
  - Comment at line 32-33 says "lesson 7 — female (cycle restarts)" — confirms intentional manual rotation, not modular. So lesson 9 falls off the documented mapping.
- **Recommendation:** Either extend the array to cover the full lesson roster (the file is hand-curated for a reason — gender alternation) or make the indexing explicitly modular and document it. Today the comment lies about what lesson 9 receives.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-26: `check-admin.ts` connects with hard-coded credentials path and `postgres-js`, parallel to the rest of the toolchain
- **Severity:** cleanup
- **Category:** inconsistency
- **Evidence:**
  - `scripts/check-admin.ts:1-36` — uses `postgres` (postgres-js) to connect directly to `api.supabase.duin.home:5432` as the `postgres` superuser.
  - All other scripts use the `@supabase/supabase-js` client over HTTPS via Kong/PostgREST.
  - The script dumps `auth.users` (line 25) if no admin row exists — that's a privileged operation invoked from a tiny ad-hoc tool.
- **Recommendation:** Replace with a Supabase JS query against `indonesian.user_roles` (service-role key) so the entire script toolkit uses one connection method and one credential.
- **Estimated effort:** trivial
- **Cross-slice dependency:** null

### F7-27: Multiple `check-*-coverage.ts` scripts duplicate paging/env/client setup
- **Severity:** nice-to-have
- **Category:** duplication
- **Evidence:**
  - `scripts/check-vocab-coverage.ts:23-56` — `selectAllRows<T>` paging helper, env load, supabase client.
  - `scripts/check-pos-coverage.ts:1-22` — `cnt` count helper, env, supabase client.
  - `scripts/check-lesson-coverage.ts:1-23` — `cnt` count helper (identical signature to `check-pos-coverage.ts`), env, supabase client.
  - `scripts/check-section-content.ts:1-12` — env, supabase client.
  - `scripts/check-exercise-coverage.ts` — staging-only, no DB.
  - Five sibling scripts repeat the same boilerplate; the `cnt` helper is reimplemented twice with different shapes.
- **Recommendation:** Extract `scripts/lib/supabase-cli.ts` with `createSupabaseClient()`, `cnt(label, qPromise)`, `selectAllRows<T>(query)`. Trim each script to ~30 lines.
- **Estimated effort:** small
- **Cross-slice dependency:** null

### F7-28: `seed-drying-scenario.ts` writes capability state directly with hand-rolled `state_version: 1` / `consecutive_failure_count: 0`
- **Severity:** nice-to-have
- **Category:** architecture-violation
- **Subtype:** pipeline-shortcut
- **Evidence:**
  - `scripts/seed-drying-scenario.ts:151-170` — upserts `learner_capability_state` rows with hand-set columns including `state_version`, `consecutive_failure_count`, `next_due_at`, `last_reviewed_at`.
  - `learner_capability_state` rows are normally produced by the capability review processor RPC (`commit_capability_answer_report` per CLAUDE.md Data Model). The script bypasses that contract for a smoke-test scenario.
  - ADR 0004 (atomic review commits) — capability state changes are intentionally only produced by the server RPC.
- **Recommendation:** Acceptable as a dev-only smoke seeder, but the file should declare the bypass explicitly and reference the canonical write path so future readers don't copy the pattern. Or invoke a series of `commit_capability_answer_report` calls instead.
- **Estimated effort:** small
- **Cross-slice dependency:** agent 6 (capability RPCs)

## Open questions for orchestrator

1. **Lesson-10 staging dir** — pre-publish state confirmed (only canonical inputs + `sections-catalog.json` + bare scaffolds; no `capabilities.ts`/`content-units.ts` yet). Should the orchestrator surface this as a "WIP authoring" status to the user, or treat it as candidate for cleanup if abandoned? F7-14 (index.ts referencing missing files) needs to be fixed regardless.
2. **Legacy lessons 1-3 catalogs in the reverse-engineered shape (F7-15)** — does the orchestrator want to align them with the canonical shape (small migration) or accept the divergence and drop the script?
3. **One-off retirement plan** — F7-3, F7-4, F7-5, F7-7, F7-9, F7-10, F7-22 are all "this was a one-shot, run, never deleted" cleanup candidates. The shipped plan `docs/plans/2026-05-08-pipeline-cleanup-for-lessons-fold.md:941` already promises retirement for some of these — should an "authoring-scripts cleanup" follow-up PR be scheduled?

## Coverage notes

- **Not deeply read** (too large or out of slice priority): `scripts/lint-staging.ts` (1,199 lines — skimmed only); `scripts/generate-section-audio.ts`, `scripts/generate-exercise-audio.ts`, `scripts/build-sections.ts`, `scripts/catalog-lesson-sections.ts`, `scripts/generate-exercises.ts` (skimmed headers + model strings; deep audit would target their LLM prompts which is non-mechanical).
- **Not in my slice but verified by reference** — `publish-approved-content.ts`, the capability-stage runner, `scripts/lib/` enrichment files (Agent 6 owns these).
- **Staging derived files** for lessons 1–9 confirmed regenerated by capability-stage runner (`// Regenerated by capability-stage runner` header on `content-units.ts`, `capabilities.ts`, `exercise-assets.ts`, `lesson-page-blocks.ts`). No evidence of hand-edits on the seven sampled lessons. Hand-edit-detection on every line would require structural diff against a fresh `publish-approved-content.ts` run — not in scope here.
- **Untracked `scripts/data/staging/lesson-10/`** — investigated. Contains the standard "first 4 steps" outputs (sections-catalog.json + scaffolds + populated learning-items.ts/lesson.ts) but **none** of the capability-stage-projected files. Consistent with a lesson mid-authoring. The `index.ts` over-promises (F7-14).
- **`spoken-variant-generator/`** — file inventory only (`rules.ts`, `transform.ts`, `types.ts` — 3 files, totalling ~11 KB). Consumer is `scripts/generate-spoken-variants.ts` (Agent 6 slice). Not deep-read.
- **`scripts/data/podcasts.ts`** — confirmed still imported by `seed-podcasts.ts:4`; live.
